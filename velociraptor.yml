# allow:
#   - write
#   - read
#   - net
#   - env

# envFile:
#   - .env

scripts:

  commitlint:
    # cmd: echo --- ${GIT_ARGS} ---
    # cmd: deno run --allow-read="./.git" commitlint.ts
    cmd: cat .git/hooks/commit-msg
    gitHook: commit-msg
